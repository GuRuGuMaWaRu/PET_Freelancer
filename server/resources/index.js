const clientRouter = require("./client/client.router");
const projectRouter = require("./project/project.router");
const userRouter = require("./user/user.router");

module.exports = { clientRouter, projectRouter, userRouter };
