import styled from "@emotion/styled";
import { colors } from "shared/const";

const SActionButton = styled.button({
  backgroundColor: "transparent",
  color: colors.white,
  border: 0,
});

export { SActionButton };
