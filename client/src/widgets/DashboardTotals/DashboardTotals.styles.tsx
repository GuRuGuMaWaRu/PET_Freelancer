import styled from "@emotion/styled";

const STotalsWrapper = styled.div({
  display: "grid",
  gridTemplateColumns: "repeat(auto-fit, minmax(200px, 1fr))",
  gridGap: "1rem",
  marginBottom: "2rem",
});

export { STotalsWrapper };
