import React from "react";

import { StyledMessage } from "../styles/notFound.styles";

const NotFound = () => {
  return <StyledMessage>404. Page Not Found.</StyledMessage>;
};

export default NotFound;
